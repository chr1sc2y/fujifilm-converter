from setuptools import setup

# This minimal setup.py exists only to help very old versions of pip
# (pre-21.3) recognize this as a setuptools-based project for editable installs.
# Modern pip + pyproject.toml is the preferred way.
if __name__ == "__main__":
    setup()
